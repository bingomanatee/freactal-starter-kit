/* eslint-disable camelcase */

ComponentImports

export default (bottle => {
  bottle.service('componentMap', () => {
    const componentMap = new Map();

    ComponentsMapping

    return componentMap;
  })
})
